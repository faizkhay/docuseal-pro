<div class="max-w-lg mx-auto px-2">
  <h1 class="text-4xl font-bold text-center mt-8"><%= t('create_your_account') %></h1>
  <%= form_for '', html: { class: 'space-y-6' }, url: registration_path do |f| %>
    <div class="space-y-2" dir="auto">
      <%= fields_for @user do |ff| %>
        <div class="grid gap-2 md:grid-cols-2 md:gap-4">
          <div class="form-control">
            <%= ff.label :first_name, t('first_name'), class: 'label' %>
            <%= ff.text_field :first_name, required: true, autofocus: true, autocomplete: 'given-name', class: 'base-input' %>
          </div>
          <div class="form-control">
            <%= ff.label :last_name, t('last_name'), class: 'label' %>
            <%= ff.text_field :last_name, required: true, autocomplete: 'family-name', class: 'base-input' %>
          </div>
        </div>
        <div class="form-control">
          <%= ff.label :email, t('email'), class: 'label' %>
          <%= ff.email_field :email, required: true, autocomplete: 'email', class: 'base-input' %>
        </div>
      <% end %>
      <%= fields_for @account do |ff| %>
        <set-timezone data-input-id="account_timezone"></set-timezone>
        <%= ff.hidden_field :timezone %>
        <div class="form-control">
          <%= ff.label :name, t('company_name'), class: 'label' %>
          <%= ff.text_field :name, required: true, autocomplete: 'organization', class: 'base-input' %>
        </div>
      <% end %>
      <%= fields_for @user do |ff| %>
        <div class="form-control">
          <%= ff.label :password, t('password'), class: 'label' %>
          <password-input class="block relative">
            <%= ff.password_field :password, required: true, autocomplete: 'new-password', placeholder: '************', class: 'base-input w-full pr-10', data: { target: 'password-input.passwordInput' } %>
            <button data-target="password-input.togglePasswordVisibility" type="button" class="absolute inset-y-0 h-12 right-0 pr-3 flex items-center">
              <span data-target="password-input.hiddenPasswordIcon">
                <%= svg_icon('eye', class: 'w-6 h-6 shrink-0 bg-white') %>
              </span>
              <span data-target="password-input.visiblePasswordIcon" class="hidden">
                <%= svg_icon('eye_off', class: 'w-6 h-6 shrink-0 bg-white') %>
              </span>
            </button>
          </password-input>
        </div>
      <% end %>
    </div>
    <div class="form-control">
      <%= f.button button_title(title: t('sign_up'), disabled_with: t('signing_up')), class: 'base-button' %>
    </div>
  <% end %>
  <div class="text-center mt-4">
    <%= link_to t('already_have_an_account'), new_user_session_path, class: 'link' %>
  </div>
</div>
